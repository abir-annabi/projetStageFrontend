/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AddSignataireComponent } from './add-signataire.component';

describe('AddSignataireComponent', () => {
  let component: AddSignataireComponent;
  let fixture: ComponentFixture<AddSignataireComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AddSignataireComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AddSignataireComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/